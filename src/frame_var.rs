/// Preset frames.
#[allow(missing_docs)]
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum FrameVar {
    Space,
    Double,
    Round,
    HorDouble,
    VertDouble,
    Heavy,
    Light,
    VertHeavy,
    HorHeavy,
}
